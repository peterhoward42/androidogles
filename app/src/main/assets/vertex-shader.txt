uniform mat4 mvpTransform;
uniform vec4 towardsLight;

attribute vec4 position;

attribute vec4 normal;

varying vec4 color;

void main() {
    gl_Position = mvpTransform * position;
    color = normal; // keep it alive if even though not used
    color = towardsLight;
}