uniform mat4 uMVPMatrix;
uniform vec4 lightDirnRev;

attribute vec4 a_Position;

attribute vec4 a_Normal;

varying vec4 vColor;

// nonsense, and is overriden below. but makes attrib used.

void main() {
    gl_Position = uMVPMatrix * a_Position;
    vColor = a_Normal; // keep it alive if even though not used
    vColor = lightDirnRev;
}